package com.seeyuan.logistics.activity;

import android.os.Bundle;
import android.util.Log;
import android.view.View;
import android.view.View.OnClickListener;
import android.widget.Button;

import com.seeyuan.logistics.R;

public class TabInsuranceInsuranceInfoActivity extends BaseActivity implements
		OnClickListener {

	// private Button BuyInsurance_InsuranceInfoNext_But;

	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_tab_insurance_insuranceinfo);

		initView();
	}

	@Override
	public void initView() {
		// BuyInsurance_InsuranceInfoNext_But = (Button)
		// findViewById(R.id.BuyInsurance_InsuranceInfoNext_But);
		// BuyInsurance_InsuranceInfoNext_But.setOnClickListener(this);
	}

	@Override
	public void onClick(View view) {
		switch (view.getId()) {

		default:
			break;
		}
	}

	@Override
	public void onClickListener(View view) {

		switch (view.getId()) {
		case R.id.BuyInsurance_InsuranceInfoNext_But:
			Log.d("TAG", "下一步");
			break;

		default:
			break;
		}

	}

}
